<% content_for :head do %>
	<%= stylesheet_link_tag "post-it"%>
<%end%>
<%= error_messages_for :talk %>
<% form_for @talk do |f| %>
	<fieldset>
		<legend>Edit Talk</legend>
		<%= render :partial => 'form', :locals => {:f => f}%>	
	</fieldset>
	<%= f.submit "Update"%>	
<%end%>