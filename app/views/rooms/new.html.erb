<%= error_messages_for :room %>
<% form_for @room do |f| %>
	<fieldset>
		<legend>New Room</legend>
		<%= render :partial => 'form', :locals => {:f => f}%>	
	</fieldset>
	<%= f.submit "Create"%>	
<%end%>